{
  "name": "api",
  "version": "1.0.0",
  "description": "API ejemplo completo",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "author": "dsaenz",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.8",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  }
}
